// Stage 0 — Brand Overview (passthrough)
// No LLM call. Input is concatenated document text + user message.
// Output is the same string passed verbatim to Stage 1.
// Handled directly by the pipeline runner.
export {};
